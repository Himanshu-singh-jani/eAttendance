name: barcode_generator
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^14.2.0
  dio: ^5.4.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2

  # 🗺️ Location tracking (FREE - No API key needed!)
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  flutter_map: ^6.1.0 # OpenStreetMap - completely free
  latlong2: ^0.9.1 # Required for flutter_map

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
